# Doppler CLI config for Chronicle Platform monorepo (local dev only).
# Run once: doppler login && doppler setup --no-interactivesetup:
  - project: chronicle-platform-frontend
    config: dev
    path: apps/frontend
  - project: chronicle-platform-backend
    config: dev
    path: backend
  - project: chronicle-env-manager
    config: dev
    path: apps/env-manager
