{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "createdAt": {
      "format": "date-time",
      "type": "string"
    },
    "id": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "slug": {
      "type": "string"
    },
    "stripeCustomerId": {
      "type": [
        "string",
        "null"
      ]
    },
    "stripePriceId": {
      "type": [
        "string",
        "null"
      ]
    },
    "stripeSubscriptionStatus": {
      "type": [
        "string",
        "null"
      ]
    },
    "updatedAt": {
      "format": "date-time",
      "type": "string"
    },
    "workosOrganizationId": {
      "description": "Linked WorkOS Organization id. Populated lazily during the WorkOS importer (Phase 1) or eagerly via `provisionWorkspace` (Phase 2).",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "createdAt",
    "id",
    "name",
    "slug",
    "updatedAt"
  ],
  "title": "Tenant",
  "type": "object"
}