// This file was generated by packages/chronicle/scripts/gen-zod.ts. Do not edit manually.
import { z } from "zod";
import type { TimelineWindow } from "../../types/timeline/TimelineWindow";

export const TimelineWindowSchema = z.object({ "events": z.array(z.object({ "actor": z.union([z.string(), z.null()]).optional(), "color": z.union([z.string().describe("Optional explicit color override; falls back to the source color."), z.null()]).optional(), "correlationKey": z.union([z.string().describe("Looser, app-defined grouping key (e.g. `conversation_id`)."), z.null()]).optional(), "id": z.string(), "message": z.union([z.string(), z.null()]).optional(), "occurredAt": z.string().datetime({ offset: true }).describe("ISO timestamp."), "parentEventId": z.union([z.string().describe("Direct causal predecessor."), z.null()]).optional(), "payload": z.any().describe("Raw payload — shown JSON-pretty in the detail panel.").optional(), "source": z.string().describe("Source/system the event came from (e.g. `intercom`, `stripe`)."), "stream": z.union([z.string().describe("Optional grouping (capture stream id) — currently informational."), z.null()]).optional(), "traceId": z.union([z.string().describe("Trace this event belongs to."), z.null()]).optional(), "traceLabel": z.union([z.string().describe("Human-friendly label for the trace."), z.null()]).optional(), "type": z.string().describe("Event type within the source (e.g. `conversation.created`).") }).describe("A single event rendered as a mark on the stream timeline. The full shape lives next to dataset shapes because `DatasetSnapshot.events` is the only consumer that ships over the wire today.")).describe("Events in the window, sorted ascending by `occurredAt`."), "from": z.union([z.string().describe("Inclusive lower bound (ISO 8601). Omitted when the request did not pin a `from` and the server returned the most-recent `limit` events."), z.null()]).optional(), "hasMore": z.boolean().describe("`true` if events older than `from` exist that the caller can page back to. Drives the timeline's \"load older\" affordance."), "to": z.union([z.string().describe("Inclusive upper bound (ISO 8601). Defaults to \"now\" when the caller does not specify one."), z.null()]).optional(), "totalCount": z.union([z.number().int().gte(0).describe("Total count if the backend can supply it cheaply, otherwise `None`. The dashboard does not require this; it's surfaced for diagnostics only."), z.null()]).optional() }).describe("A page of timeline events. The response carries the window boundaries that produced it so clients can stitch successive pages back together (and so the playhead anchor on first paint uses the server's clock, not the browser's).")

// Static drift guard — fails compilation if Rust + Zod diverge.
type _TimelineWindowInferred = z.infer<typeof TimelineWindowSchema>;
// eslint-disable-next-line @typescript-eslint/no-unused-vars
type _TimelineWindowDriftCheck = _TimelineWindowInferred & TimelineWindow;
