// This file was generated by packages/chronicle/scripts/gen-zod.ts. Do not edit manually.
import { z } from "zod";
import type { DatasetPurpose } from "../../types/datasets/DatasetPurpose";

export const DatasetPurposeSchema = z.enum(["eval","training","replay","review"]).describe("Intended use of a dataset — drives the colored badge on the picker and lets apps route additions to the right backend (eval suite, training set, replay corpus, manual review queue).")

// Static drift guard — fails compilation if Rust + Zod diverge.
type _DatasetPurposeInferred = z.infer<typeof DatasetPurposeSchema>;
// eslint-disable-next-line @typescript-eslint/no-unused-vars
type _DatasetPurposeDriftCheck = _DatasetPurposeInferred & DatasetPurpose;
